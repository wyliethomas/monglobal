<h1>New Post</h1>

<%= form_for(:post, :url => translate_posts_path(params[:locale]), :html => {:multipart => true, :class => :crud_form}) do |f| %>
  <%= render :partial => 'form', :locals => {:f=>f} %>
  <p><%= f.submit "Add", :class => "submit" %></p>
<% end %>


